[Service]
# If nginx fails to start (e.g. brief DNS blip during apt reload), retry instead of
# leaving :443 down until someone SSHes in.
Restart=on-failure
RestartSec=30s
StartLimitIntervalSec=600
StartLimitBurst=10
