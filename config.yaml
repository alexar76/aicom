# =============================================================================
# AI-Factory — deployment overlay (optional)
# =============================================================================
# Defaults are loaded from ``config/fragments/*.yaml`` (bundled), then this file
# is merged on top. Admin → Settings persists keys here via the FastAPI AppConfig.
# See docs/configuration.md
# =============================================================================

{}
