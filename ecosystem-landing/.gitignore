.claude/
.cursor/
.DS_Store

# Generated by scripts/build_seo_landings.py — built in CI / deploy, not committed.
# The builder emits English at the root and every other language under /{lang}/, so
# these must NOT be root-anchored: leading-slash patterns let 148 generated pages
# under ru/ es/ fr/ zh/ into git, where they drifted against the generator.
learn/
oracles/
guides/
encyclopedia/
shared/
sitemap.xml
robots.txt
