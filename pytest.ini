[pytest]
testpaths = tests
norecursedirs = data .git .venv node_modules web/frontend __pycache__
addopts = --strict-markers
markers =
    integration: live HTTP probes against the running ecosystem fleet (ECOSYSTEM_INTEGRATION=1)
    asyncio: async test marker
