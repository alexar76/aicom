# Benchmark prompts — full_software (dashboard · auth · API · DB)

SaaS for remote teams with dashboard, JWT auth, REST API, Postgres or SQLite, tasks CRUD, and settings page.

Echo-style voice notes app with backend sync, user accounts, and encrypted blob metadata API.

Privacy-first habit tracker with login, data export API (JSON/CSV), and optional Postgres analytics sink.
