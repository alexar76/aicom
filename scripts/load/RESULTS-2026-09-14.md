# Сводка нагрузочных прогонов — 2026-09-14

Публичный флот, Locust 2.46.5 с ноутбука. Адреса серверов **не** публикуются: только SSH-алиас и публичный hostname. Карта: [`topology.py`](topology.py).

Pulse перед прогоном поднят: `https://pulse.modelmarket.dev/` и `https://magic-ai-factory.com/pulse/` — HTTP 200, TLS Let’s Encrypt, `certbot.timer` включён.

## Кто куда бьёт

| Сервер (SSH) | Модули |
|---|---|
| Factory `my-vps` | Factory, Hub `modelmarket.dev`, Mesh, ARGUS `/arena`, Monitor, Pulse, ATLAS, THEMIS, forge, verify |
| Oracles `admin-vps` | GAIA `iot`, oracles/Platon, lottery, MOMUS, LOGOS |
| Metis `skopos.modelmarket.dev` | Metis `/health`, SKOPOS |
| Hub lab `competing-lab` | **не в смеси** (live Hub — на factory) |

HTML/CSV: `scripts/load/reports/` (gitignored).

## Результаты

### smoke — core, 8 VU, 90 с

511 запросов, **0% fail**. Pulse 200. Factory catalog/trust-metrics медленные (~4 с), не 5xx.

### constant — core, 20 VU, 10 мин

8195 запросов, **1 fail (0.01%)**: Mesh `GET /v1/activity` — `RemoteDisconnected`. Locust exit 1 из‑за `--exit-code-on-error`.

Хвост на **factory**: `/api/products` и trust-metrics p50 ~6–7 с, p95 ~9 с. Monitor p95 до ~9 с. Pulse/ARGUS/Mesh (кроме того одного обрыва) зелёные.

### ramp — core, 4→80 VU

12192 запроса, **0% fail**. Форма дошла до потолка 80 VU и остановилась (ёмкость по латентности, не по ошибкам).

На 80 VU (всё на factory): Factory products p50 ~15 с / max ~30 с; Monitor p99 ~25 с. Mesh/ARGUS/Pulse без 5xx.

### full — core+флот, 24 VU, 10 мин

9674 запроса, **0% fail**, exit 0. Preflight всех трёх хостов 200.

Спутники (oracles / Metis) держат p95 < 1 с на health. Узкое место по-прежнему **factory**: products p95 ~12 с, trust-metrics ~9 с, Monitor health хвост ~10–12 с, `ecosystem-status` p95 ~8 с.

Pulse на поддомене: med ~54 мс, 0 fail.

### heavy #1 — 2 VU, 90 с (неполный, не засчитан)

Задумано: ARGUS `/ask`, Hub invoke, Mesh `POST /v1/tasks`, Metis `POST /v1/verify`, Factory pipeline.

Факт: **3 GET** `factory GET /ai-market/pipelines`, 0 fail. POST не ушли.

Почему:

1. 2 VU при пяти тяжёлых классах — Locust посадил только `ArgusAskUser` + `FactoryPipelineUser`. Hub/Mesh/Metis — 0 VU.
2. `ARGUS_HTTP_TOKEN` с factory-контейнера не попал в env (`token=` пустой) — `/ask` скипнулся.
3. `FactoryPipelineUser` в 90 с успел только list (вес list 3 : create 1, wait 25–45 с).

### heavy #2 — 5 VU, 3 мин (все пять классов)

49 запросов, **20 fail (40.82%)**, Locust exit 1. HTML/CSV: `reports/heavy-20260914T053700Z`. Preflight core 200. `ARGUS_HTTP_TOKEN` снова пустой после `ssh my-vps docker exec argus printenv` — `POST /ask` не ушёл.

| Запрос | Сервер | # | fail | med |
|---|---|---|---|---|
| Mesh `POST /v1/tasks` | factory | 12 | 0 | ~510 мс |
| Factory `GET /ai-market/pipelines` | factory | 11 | 0 | ~69 мс |
| Factory `POST /ai-market/pipelines` | factory | 6 | 0 | ~89 мс |
| Hub `POST /ai-market/v2/invoke` | factory | 9 | **9 / 422** | ~61 мс |
| Metis `POST /v1/verify` | metis | 11 | **11 / 401** | ~86 мс |
| ARGUS `POST /argus/ask` | factory | 0 | skipped | — |

Mesh tasks и factory pipeline create на этом объёме зелёные. Hub invoke — схема/тело (422, не 5xx). Metis verify — нет ключа (401). ARGUS `/ask` этим прогоном **не измерен**.

## Выводы

1. Read-path **живёт**. Ошибки почти нулевые; ломается latency на factory catalog/Monitor, не оркестрация 5xx.
2. Спутники на oracles и Metis в смеси full не деградировали. Нельзя читать factory-хвост как «упал весь флот».
3. Pulse после восстановления не был узким местом ни в одном режиме.
4. Heavy POST: Mesh tasks и factory pipeline create на 5 VU / 3 мин живые. Hub invoke 422 (тело), Metis verify 401 (ключ), ARGUS `/ask` скипнут — токен с `argus` контейнера на factory в laptop env не попал.
5. KI-3 (uvicorn soak pipeline create) этим прогоном **не закрыт** — шесть create, не soak.

## Pass/fail vs методика

| Режим | Критерий | Итог |
|---|---|---|
| smoke | fail-ratio < 5%, health p95 < 2 с | **pass** (health ок; catalog медленный, не fail) |
| constant | fail-ratio < 5% | **pass** (0.01%, один обрыв Mesh) |
| ramp | стоп по fail-ratio ≥ 8% или потолок | **потолок 80 VU, 0% fail** — излом по latency factory/Monitor |
| full | то же на трёх хостах | **pass** 0% fail |
| heavy | реальные POST | **частично** — Mesh + pipeline OK; Hub 422; Metis 401; ARGUS skip |
