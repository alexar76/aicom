Initial public release of **Treasury** — separate payer for MOMUS bounties.

- Holds the only payout key (MOMUS never sees it)
- Independent verification before release
- GitHub Pages landing + CI
- Vendors `oracle-core` + `momus` for standalone Docker

Landing: https://alexar76.github.io/treasury/
