## AIMarket Agent (Python client) v0.1.0 (early)

**What works today**
- Python client for discovering and invoking hub capabilities
- pytest CI on GitHub Actions
- Mirrors monorepo `aimarket-agent/` package

**Unstable / may change**
- Package version in `pyproject.toml` vs this Git tag (0.x public API policy)
- Hub invoke/auth headers and capability manifest fields

**Not in this release**
- PyPI publish automation; install from source or git tag for now
