# lottery v0.1.0

Release from the AICOM monorepo mirror (`main`), capturing changes since 2026-05-20.

## Changes
- fix(hub): Postgres-portable calibration SQL + multi-worker slash safety
- feat(ecosystem): THEOROS collab on landing, relay scout ops, fleet updates
- sync from Gitea#2: oracles, platon, lottery
- fix(security): comprehensive audit fix — 20 files, 29 findings closed
- fix(security): full ecosystem audit — 43 verified findings across two passes
- fix(lottery): use draw_word in fulfillDraw after Sortes migration
- feat: add lottery badges and CI scripts
- feat(oracles): 17-oracle family (6 new) + ARGUS conscience, live 3D previews
- test(argus,lottery): cover modular cli, studio, and economy splits.
- refactor(argus,lottery): split god modules into focused packages.
- feat(demo): passwordless public admin login; drop demo123 gate
- sync from Gitea: oracles, lottery, alien-monitor (overlay pull)
- fix(monitor): sparse 3D graph layout and unify node anchors
- sync from Gitea#2: oracles, platon, lottery — cleanup + simplification
- feat(deploy): single canonical Base-address registry, auto-loaded on crypto+base
- feat(crypto): ecosystem-wide master switch — crypto OFF by default, UNI on
- docs+ops: trilingual SDK/ecosystem guides + LIVE-against-Base deploy + realistic UNI cadence
- fix(lottery-deploy): drop stale `forge install --no-commit` flag (deploy was failing)
- fix(lottery-ui): DEMO sim no longer leaks into LIVE/UNI — each mode reflects its reality
- feat(chain): unified EVM+Solana network registry + health-checked RPC failover

Source: monorepo path `lottery` · https://github.com/alexar76/lottery
