## AIMarket Plugins v0.1.0 (early)

**What works today**
- 15 hub plugins (TEE, channels, reputation, safety, auction, ZK, and more)
- `aimarket-provenance` plugin from hub export layout
- pytest plugins CI

**Unstable / may change**
- Per-plugin `pyproject.toml` versions vs ecosystem 0.x release line
- Hub plugin load paths and config schema

**Not in this release**
- PyPI per-plugin publish; use monorepo mirror / git tag
