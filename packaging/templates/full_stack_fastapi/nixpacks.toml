# Railway Nixpacks hints — Python FastAPI (matches Dockerfile if you deploy via Dockerfile instead).
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
